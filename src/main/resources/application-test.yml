
spring:
  datasource:
    url: "dbc:h2:mem:mydb"
    driverClassName: 'org.h2.Driver'
    platform: 'postgres'
    username: 'sa'
    password: 'password'

  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
